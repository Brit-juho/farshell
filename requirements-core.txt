fastapi>=0.110
uvicorn[standard]>=0.29
python-multipart>=0.0.9
websockets>=12.0
pynacl>=1.5
pytest>=8.0
httpx>=0.27  # pytest + FastAPI testclient용
textual>=0.50  # vt manage TUI (W4-1)
pywebpush>=1.14      # Web Push (P5) — VAPID 서명 + 페이로드 암호화
