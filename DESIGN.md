# 랄프톤 디자인 시스템

## 테마: Catppuccin Mocha

| 토큰 | 값 | 용도 |
|------|-----|------|
| `--bg` | `#1e1e2e` | 배경, 터미널 |
| `--surface` | `#181825` | 탭 바, 보이스 바 |
| `--overlay` | `#313244` | 카드, 토스트, 드롭다운 |
| `--muted` | `#45475a` | 버튼 기본, 구분선 |
| `--subtle` | `#585b70` | 활성 탭, 호버 |
| `--text` | `#cdd6f4` | 본문 텍스트 |
| `--subtext` | `#a6adc8` | 보조 텍스트, 힌트 |
| `--red` | `#f38ba8` | 에러, 녹음 중, 연결 끊김 |
| `--green` | `#a6e3a1` | 성공, 연결됨, 업로드 완료 |
| `--blue` | `#89b4fa` | 정보, 링크 |
| `--lavender` | `#cba6f7` | 음성 전용 모드 활성 |

## 타이포그래피

- 폰트: `system-ui, sans-serif` (UI), 터미널은 xterm.js 기본 모노스페이스
- 데스크톱 터미널: 14px
- 모바일 터미널: 12px
- UI 텍스트: 13px
- 토스트/알림: 13px

## 스페이싱

- 탭 패딩: `8px 14px`
- 보이스 바 패딩: `8px 12px`
- 버튼 간격: `10px`
- 토스트 패딩: `10px 20px`
- border-radius: 버튼 `50%` (원형), 카드/토스트 `8px~10px`, 탭 `8px`

## 컴포넌트

### 버튼 (보이스 바)
- 원형: `48px` (기본), `120px` (음성 전용 모드)
- 배경: `--muted`, 활성 시 역할별 색상
- 녹음 중: `--red` + pulse 애니메이션
- 핸즈프리: `--green` (활성)
- 음성 전용: `--lavender` (활성)

### 토스트
- 위치: 보이스 바 위 (`bottom: 80px`)
- 배경: `--overlay`
- 색상: `--blue` (정보), `--red` (에러), `--green` (성공)
- 자동 사라짐: 4초

### 탭
- 기본: `--overlay` 배경
- 활성: `--subtle` 배경, `#f5e0dc` 텍스트
- 닫기 버튼: 우측 `×`

### 드롭다운 (tmux 메뉴)
- 배경: `--overlay`
- 호버: `--muted`
- 그림자: `0 4px 12px rgba(0,0,0,0.5)`

### 검색 바
- 위치: 우측 상단 고정
- 입력: `--bg` 배경, `--subtle` 테두리
- 버튼: `--muted` 배경

## 모바일 (375px 기준)

- safe-area-inset 적용
- 터미널 폰트: 12px
- 탭 바: 가로 스크롤
- 보이스 바: 하단 고정
- 음성 전용 모드: 터미널/탭 숨기고 보이스 바 전체 화면

## 접근성

- 터치 타겟: 최소 44px (보이스 바 버튼 48px)
- 토스트: 4초 후 자동 사라짐 (충분한 읽기 시간)
- 녹음 상태: 시각(색상+애니메이션) + 텍스트("녹음 중...")
